---
description: "Generate shortform video hooks + script for a SaaS site (free, local)"
---

The user wants shortform video hooks and a script for: **$ARGUMENTS**

Use the `clipus-shortform-script` skill to produce the output. If `$ARGUMENTS` is a URL and you can fetch it, extract the real value proposition and features first so the script is grounded in the actual product. If nothing was provided, ask for a URL or a short product description.
